services:
  rebot:
    build:
      context: .
      dockerfile: ./dockerfile
    restart: always
    logging:
      driver: local
    env_file:
      - .env
    # environment:
    #   - REBOT_DISCORD_TOKEN=editme
    #   - REBOT_GEMINI_TOKEN=editme
